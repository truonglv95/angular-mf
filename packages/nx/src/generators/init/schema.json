{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Module Federation Init Generator",
  "description": "Initializes Module Federation in an Nx workspace project.",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project to add Module Federation to.",
      "$default": {
        "$source": "projectName"
      }
    },
    "type": {
      "type": "string",
      "description": "The type of the application (host or remote).",
      "enum": ["host", "remote"],
      "default": "host"
    },
    "remotes": {
      "type": "array",
      "description": "List of remote applications (only applicable if type is host).",
      "items": {
        "type": "string"
      },
      "default": []
    }
  },
  "required": ["project"]
}
