{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Module Federation Application Executor",
  "description": "Builds an Angular application with Module Federation support.",
  "type": "object",
  "properties": {
    "mfConfig": {
      "type": "string",
      "description": "Path to the Module Federation configuration file."
    }
  },
  "additionalProperties": true
}
